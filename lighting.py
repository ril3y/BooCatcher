#lighting Code
  # while 1:
    #     pixels.fill(get_random_color())
    # print("Bennetts Coffin Program!")
    # while(1):
    #     for i in range(0, pixel_numbers):
    #     #     print("Set %d pixel" % i)
    #     #     pixels[i] = BLUE
    #         print("Set %d pixel" % i)
    #         if (i % 2) == 0:
    #             pixels[i] = get_random_color()
    #             sleep(SLEEP_TIME)
    #     for i in range(0, pixel_numbers):
    #         print("Set %d pixel" % i)
    #         if (i % 2) == 1:
    #             pixels[i] = get_random_color()
    #             sleep(SLEEP_TIME)


        #sleep_time = random.randint(0,5)
        #print("Sleeping for: %d " % sleep_time)
        #sleep(sleep_time)
        #lightning(-1)
        #pixels.fill((0, 0, 0))
